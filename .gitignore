# Don't check auto-generated stuff into git
coverage
node_modules

# Build stuff
dist
.prepublish

# Locks - one lock is enough. Might go out of sync
yarn.lock
shrinkwrap.yaml

# Cruft
.DS_Store
npm-debug.log
.idea

# Logs and stats
stats.json
prod-build-stats.json

yarn-error.log
